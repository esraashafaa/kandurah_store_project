# دليل الترجمة والتنقل بين اللغتين في الداشبورد

## 📋 نظرة عامة

المشروع يستخدم نظام الترجمة المدمج في Laravel لدعم اللغتين العربية والإنجليزية. هذا الدليل يشرح كيفية عمل النظام الحالي وكيفية تحسينه.

---

## 🔍 الوضع الحالي

### 1. **ملفات الترجمة الموجودة**

```
lang/
├── ar/
│   ├── orders.php
│   ├── reviews.php
│   ├── transactions.php
│   └── users.php

resources/lang/
└── ar/
    ├── orders.php
    └── users.php
```

### 2. **الإعدادات الحالية**

في `config/app.php`:
- `locale` => `env('APP_LOCALE', 'en')` - اللغة الافتراضية
- `fallback_locale` => `env('APP_FALLBACK_LOCALE', 'en')` - اللغة الاحتياطية

### 3. **المشاكل الحالية**

1. ❌ **نصوص عربية مكتوبة مباشرة في الـ Views** بدلاً من استخدام ملفات الترجمة
2. ❌ **لا يوجد مبدل لغة (Language Switcher)** في الواجهة
3. ❌ **لا يوجد Middleware** لتغيير اللغة
4. ❌ **لا يوجد Route** لتبديل اللغة
5. ❌ **اتجاه الصفحة (dir) ثابت** على `rtl` في الـ Layouts

---

## 🎯 الحل المقترح

### **الخطوة 1: إنشاء Middleware لتغيير اللغة**

سيتم إنشاء Middleware يتحقق من اللغة المطلوبة ويضبطها تلقائياً.

**كيف يعمل:**
- يتحقق من وجود `locale` في الـ Session
- يتحقق من وجود `locale` في الـ URL (مثل: `/ar/dashboard`)
- يضبط `app()->setLocale()` بناءً على ذلك
- يحفظ اللغة في الـ Session للمحافظة عليها

### **الخطوة 2: إنشاء Route لتبديل اللغة**

سيتم إنشاء Route مثل:
```php
Route::get('/language/{locale}', [LanguageController::class, 'switch'])
    ->name('language.switch');
```

**كيف يعمل:**
- يستقبل اللغة المطلوبة (`ar` أو `en`)
- يحفظها في الـ Session
- يعيد توجيه المستخدم للصفحة السابقة

### **الخطوة 3: إنشاء ملفات الترجمة للداشبورد**

سيتم إنشاء ملفات ترجمة شاملة تحتوي على جميع النصوص:

```
lang/
├── ar/
│   └── dashboard.php    ← جديد
└── en/
    └── dashboard.php    ← جديد
```

**مثال على محتوى `lang/ar/dashboard.php`:**
```php
return [
    'welcome' => 'مرحباً بك',
    'total_users' => 'إجمالي المستخدمين',
    'total_orders' => 'إجمالي الطلبات',
    // ... إلخ
];
```

**مثال على محتوى `lang/en/dashboard.php`:**
```php
return [
    'welcome' => 'Welcome',
    'total_users' => 'Total Users',
    'total_orders' => 'Total Orders',
    // ... إلخ
];
```

### **الخطوة 4: تحديث الـ Views لاستخدام الترجمة**

**قبل:**
```blade
<h1>مرحباً بك، {{ Auth::user()->name }}</h1>
<p>إجمالي المستخدمين</p>
```

**بعد:**
```blade
<h1>{{ __('dashboard.welcome') }}, {{ Auth::user()->name }}</h1>
<p>{{ __('dashboard.total_users') }}</p>
```

### **الخطوة 5: إضافة مبدل اللغة في الواجهة**

سيتم إضافة زر/قائمة منسدلة في الـ Header للتبديل بين اللغتين:

```blade
<!-- في layouts/admin.blade.php -->
<div class="flex items-center gap-2">
    <a href="{{ route('language.switch', 'ar') }}" 
       class="px-3 py-1 rounded {{ app()->getLocale() === 'ar' ? 'bg-indigo-600 text-white' : 'text-gray-600' }}">
        عربي
    </a>
    <a href="{{ route('language.switch', 'en') }}" 
       class="px-3 py-1 rounded {{ app()->getLocale() === 'en' ? 'bg-indigo-600 text-white' : 'text-gray-600' }}">
        English
    </a>
</div>
```

### **الخطوة 6: تحديث Layouts لدعم الاتجاه الديناميكي**

**قبل:**
```blade
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" dir="rtl">
```

**بعد:**
```blade
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" 
      dir="{{ app()->getLocale() === 'ar' ? 'rtl' : 'ltr' }}">
```

---

## 🔄 آلية العمل الكاملة

### **1. عند زيارة المستخدم للموقع:**

```
المستخدم يزور /dashboard
    ↓
Middleware يتحقق من اللغة في Session
    ↓
إذا لم توجد → يستخدم اللغة الافتراضية (en)
    ↓
يضبط app()->setLocale()
    ↓
View يتم تحميله باللغة المحددة
```

### **2. عند تغيير اللغة:**

```
المستخدم يضغط على "عربي" أو "English"
    ↓
يتم التوجيه إلى /language/ar أو /language/en
    ↓
LanguageController يحفظ اللغة في Session
    ↓
يعيد التوجيه للصفحة السابقة
    ↓
Middleware يقرأ اللغة الجديدة من Session
    ↓
الصفحة تظهر باللغة الجديدة
```

### **3. استخدام الترجمة في الكود:**

**في Blade Views:**
```blade
{{ __('dashboard.welcome') }}
{{ __('dashboard.total_users') }}
{{ trans('dashboard.welcome') }}  <!-- طريقة بديلة -->
```

**في Controllers:**
```php
$message = __('dashboard.welcome');
```

**مع متغيرات:**
```blade
{{ __('dashboard.welcome_user', ['name' => Auth::user()->name]) }}
```

**في ملف الترجمة:**
```php
'welcome_user' => 'مرحباً بك، :name',
```

---

## 📁 هيكل الملفات المطلوب

```
app/
└── Http/
    ├── Controllers/
    │   └── LanguageController.php    ← جديد
    └── Middleware/
        └── SetLocale.php             ← جديد

lang/
├── ar/
│   ├── dashboard.php                 ← جديد
│   ├── sidebar.php                   ← جديد
│   ├── orders.php                    ← موجود
│   └── ...
└── en/
    ├── dashboard.php                 ← جديد
    ├── sidebar.php                   ← جديد
    └── ...

resources/views/
└── components/
    └── language-switcher.blade.php   ← جديد (اختياري)
```

---

## 🎨 مثال عملي: ترجمة صفحة الداشبورد

### **ملف الترجمة `lang/ar/dashboard.php`:**

```php
return [
    'title' => 'لوحة التحكم',
    'welcome' => 'مرحباً',
    'welcome_user' => 'مرحباً بك، :name',
    'stats' => [
        'total_users' => 'إجمالي المستخدمين',
        'total_orders' => 'إجمالي الطلبات',
        'total_revenue' => 'إجمالي الإيرادات',
        'total_designs' => 'إجمالي التصاميم',
    ],
    'quick_actions' => [
        'add_coupon' => 'إضافة كوبون',
        'orders' => 'الطلبات',
        'users' => 'المستخدمين',
        'designs' => 'التصاميم',
    ],
];
```

### **ملف الترجمة `lang/en/dashboard.php`:**

```php
return [
    'title' => 'Dashboard',
    'welcome' => 'Welcome',
    'welcome_user' => 'Welcome, :name',
    'stats' => [
        'total_users' => 'Total Users',
        'total_orders' => 'Total Orders',
        'total_revenue' => 'Total Revenue',
        'total_designs' => 'Total Designs',
    ],
    'quick_actions' => [
        'add_coupon' => 'Add Coupon',
        'orders' => 'Orders',
        'users' => 'Users',
        'designs' => 'Designs',
    ],
];
```

### **استخدام في `resources/views/admin/dashboard.blade.php`:**

```blade
<h1>{{ __('dashboard.welcome_user', ['name' => Auth::user()->name]) }}</h1>
<div class="stat-card-label">{{ __('dashboard.stats.total_users') }}</div>
<a href="...">{{ __('dashboard.quick_actions.add_coupon') }}</a>
```

---

## ✅ الخطوات التالية للتنفيذ

1. ✅ إنشاء `SetLocale` Middleware
2. ✅ إنشاء `LanguageController`
3. ✅ إضافة Route لتبديل اللغة
4. ✅ إنشاء ملفات الترجمة (`dashboard.php`, `sidebar.php`)
5. ✅ تحديث جميع الـ Views لاستخدام `__()`
6. ✅ إضافة مبدل اللغة في الـ Header
7. ✅ تحديث Layouts لدعم `dir` ديناميكي
8. ✅ اختبار التبديل بين اللغتين

---

## 🔧 ملاحظات مهمة

1. **اللغة المحفوظة في Session** تبقى حتى يغلق المستخدم المتصفح أو يغير اللغة
2. **يمكن حفظ اللغة في قاعدة البيانات** (في جدول `users`) للمحافظة عليها بشكل دائم
3. **يمكن استخدام Prefix في الـ Routes** مثل `/ar/dashboard` و `/en/dashboard` (اختياري)
4. **يجب ترجمة جميع الرسائل** بما فيها رسائل الخطأ والنجاح

---

## 📝 مثال كامل: LanguageController

```php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\Support\Facades\App;
use Illuminate\Support\Facades\Session;

class LanguageController extends Controller
{
    public function switch($locale)
    {
        // التحقق من صحة اللغة
        if (!in_array($locale, ['en', 'ar'])) {
            $locale = 'en';
        }

        // حفظ اللغة في Session
        Session::put('locale', $locale);
        
        // ضبط اللغة الحالية
        App::setLocale($locale);

        // إعادة التوجيه للصفحة السابقة
        return redirect()->back();
    }
}
```

---

## 🎯 الخلاصة

نظام الترجمة سيعمل كالتالي:

1. **Middleware** يضبط اللغة تلقائياً عند كل طلب
2. **Controller** يتعامل مع طلبات تغيير اللغة
3. **ملفات الترجمة** تحتوي على جميع النصوص
4. **Views** تستخدم `__()` لعرض النصوص المترجمة
5. **مبدل اللغة** في الواجهة يسمح للمستخدم بالتبديل بسهولة
6. **الاتجاه (RTL/LTR)** يتغير تلقائياً حسب اللغة

هذا النظام يضمن:
- ✅ سهولة الصيانة (تغيير النص في مكان واحد)
- ✅ دعم كامل للغتين
- ✅ تجربة مستخدم سلسة
- ✅ قابلية التوسع لإضافة لغات أخرى لاحقاً

